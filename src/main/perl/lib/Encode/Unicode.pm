package Encode::Unicode;
use strict;
use warnings;
our $VERSION = '2.18';

# Minimal stub for Encode::Unicode.
# PerlOnJava handles UTF-16/UTF-32 encoding natively in Java,
# so this module only needs to exist for modules that 'use' it.

1;
