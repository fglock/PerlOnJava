--- lib/Test/FailWarnings.pm
+++ lib/Test/FailWarnings.pm
@@ -62,6 +62,7 @@
         my ( $pkg, $filename, $line ) = caller( $i++ );
         return caller( $i - 2 ) unless defined $pkg;
         next if $pkg =~ /^(?:Carp|warnings)/;
+        next if $filename =~ m{(?:^|/)Warnings\.java$};
         return ( $pkg, $filename, $line );
     }
 }
