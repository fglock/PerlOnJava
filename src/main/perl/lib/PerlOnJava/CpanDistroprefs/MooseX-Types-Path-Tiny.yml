---
comment: |
  PerlOnJava distroprefs for MooseX::Types::Path::Tiny.

  The distribution's imported Path types, coercions, and runtime behavior pass.
  One diagnostic assertion expects a fully-qualified Moose type alias that
  PerlOnJava does not currently expose.  Catalyst consumers import the types by
  name, so allow installation despite that isolated namespace lookup mismatch.
match:
  distribution: "^ETHER/MooseX-Types-Path-Tiny-"
test:
  commandline: "PERLONJAVA_TEST_IGNORE_FAILURES"
