---
comment: |
  PerlOnJava does not implement fork. Skip Net::Server's fork-dependent test
  programs before they emit a failing `ok(can_fork())` assertion. The module
  sources and all non-fork tests remain unmodified.
match:
  distribution: "^BBB/Net-Server-"
patches:
  - "Net-Server/SkipForkTests.patch"
