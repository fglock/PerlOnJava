#!/usr/bin/perl
#
# perldoc - Look up Perl documentation in Pod format
#
# This "perldoc" file was adapted from the standard Perl distribution

require 5;
BEGIN {
    $^W = 1 if $ENV{'PERLDOCDEBUG'};
    pop @INC if $INC[-1] eq '.';
}
use Pod::Perldoc;
exit( Pod::Perldoc->run() );
